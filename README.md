Example Gymnasium RL Request Via Websockets

update apt
```sudo apg-get update && sudo apt-get swig```
install reqs
```pip install -r requirements.txt```
for cart
```python -m uvicorn main:app --reload```
for biped
```python -m uvicorn biped:app --reload```
